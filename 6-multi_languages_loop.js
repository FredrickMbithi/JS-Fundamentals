#!/usr/bin/node

// Create an array (list) of sentences
const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Loop through each sentence and print it
for (let i = 0; i < 3; i++) {
  console.log(languages[i]);
}